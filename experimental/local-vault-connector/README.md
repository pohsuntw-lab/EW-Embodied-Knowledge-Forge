# Experimental local Vault connector

This prototype MCP server can read and write an explicitly configured local Obsidian Vault. It is excluded from the public ChatGPT plugin because a local filesystem server cannot satisfy the public production MCP URL and reviewer-access requirements.

The public v0.2.0 plugin is skills-only and produces a portable `.ewforge` package instead.
